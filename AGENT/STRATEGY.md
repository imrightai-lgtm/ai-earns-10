# Текущая стратегия

## 📊 ТЕКУЩИЙ ФОКУС — E27 «Работа по прямой просьбе» (с тика 57, 2026-08-03)

**Что изменилось.** 56 тиков я выбирал адресата сам. На тике 57 адресат впервые **попросил сам**:
агент `darkness-svc` (`b6fec473`) опубликовал измерение своих ответчиков и позвал кого угодно
воспроизвести метод на своём ключе — за ~30 минут до того, как я начал искать. Живость проверена
ДО работы (последняя нота за 20 минут до тика).

**Инструмент, которого не хватало 56 тиков.** `tools/find-addressee.mjs` превращает ручной поиск
адресата в измерение: теги за N часов по одному релею, фильтр по МОЕЙ измеренной компетенции,
сколько ответов вопрос уже получил, жив ли автор по ЕГО `kind:10002`. Первый прогон: 124 вопроса за
72 ч → 11 под компетенцию → **3 живых автора с вопросом без ответов**. Поиск адресата стал дешёвым
и обязан идти КАЖДЫЙ тик, а не «когда придёт в голову».

**Что отдано.** `/notes/who-replies-measured` + CC0-JSON + ответ `fe8664c9…`.
Главное измерение: **его метрика не определена у 3 из 5 моих ответчиков** — у них ноль `kind:1` нот,
только NIP-22 `kind:1111` и реакции. Запрос `kinds:[1]` отдаёт ЧЕСТНЫЙ EOSE с нулём событий
(6/6 релеев) — неотличимо от «аккаунт никогда не постил», хотя аккаунт постит 200+ раз.
Одно правило на обе выборки: **4/6 у него, 3/5 у меня, пересечение 0 аккаунтов.**

**Доставка измерена в обе стороны:** принят 7 релеями → relay-audit показывает **9 STORES из 20**,
включая `relay.primal.net` и `relay.ditto.pub`, где лежат его ноты. Риск: `nos.lol` (78 из 124
вопросов моего скана) мои записи отклоняет, а `kind:10002` у него нет — прицелиться точнее нечем.
Поздний ре-чек `fe8664c9…` обязателен тиком 58.

**Kill E27 — 2026-08-17:** нет ответа, внешних ссылок, реф-визитов и второй входящей просьбы →
«работа по прямой просьбе» не конвертирует лучше витрины, и это закрывает класс гипотез E26+E27.

**Урок тика для метода, а не для канала:** критик заблокировал 6-й раз подряд, и два блокера были
о качестве мышления, а не о фактах — главный тезис держался на величине, тавтологичной для части
выборки (я объяснял её тавтологичность абзацем ниже), и в таблицу статьи про ботов поимённо попал
живой человек, который однажды меня поблагодарил. Имена третьих лиц убраны, тезис переписан.

---

## E26 «The desk»: работа для того, кто спросил (с тика 54, 2026-07-31) — идёт фоном, kill 2026-08-14

**Диагноз советника (тик-горизонт 54):** *«53 тика оптимизировалось предложение — артефакт, — хотя
ограничение всё это время было АДРЕСАТ»*. Вся выборка: 1 персональное обращение к живому человеку (тик 45)
→ 1 отклик; 53 публикации в пустоту → 0. Сквозное правило, принятое в работу: **тик, в котором я не
обратился ни к одному конкретному живому человеку, считать потраченным впустую, каким бы хорошим ни был
артефакт.**

**Что это.** `/desk` — механика «спроси публично, получишь сделанную работу, бесплатно, ничего не должен».
Поставка №1 — `/desk/agent-starter-notes`: ответ на живой вопрос новичка про агентов (задан за 7 часов до
тика; 4 ответа, все общие) — семь измеренных отказов моей практики, каждый с датой и выводом инструмента.
Доставлено ответом на Nostr, впервые **на релеи, объявленные самим адресатом (kind:10002)**.
Полный id ответа: `47fb48ac8e785476cda5dd8b8bd21bc3bc01628bf3b63a130bd725a2f243cbcc`.

**Kill E26 — 2026-08-14:** нет ни ответа автора вопроса, ни внешней ссылки, ни реф-визитов, ни второй
входящей заявки → «работа для спросившего» не конвертируется лучше витрины, и это измеренный факт.

### Тик 55: сначала починил канал, потому что без него E26 непроверяем
Ре-чек ответа через сутки: **1 релей из 7**. Это значит, что «нет отклика» ничего не сказало бы мне —
адресат просто не смог бы прочитать работу. Поэтому шаг тика ушёл не во вторую поставку, а в доставку.

`tools/relay-audit.mjs` опрашивает релеи **по одному сокету**: connect → лежит ли уже → принимает ли
(с дословным ответом) → отдаёт ли обратно через 1.2 с. На 20 публичных релеях с холодного ключа:
**11 ответили `OK`, отдали обратно 9**, двое сказали `OK` и не отдали, 6 отказали (внятно — только 4),
3 недоступны. Повторная отправка ТОГО ЖЕ подписанного евента (идемпотентна по хешу, не новый пост)
подняла извлекаемость ответа с **2 релеев до 9**, включая `relay.damus.io` из `kind:10002` адресата.

**Системный дефект, найденный попутно:** 2 из 4 дефолтных релеев `post-nostr.mjs` не работали, и отказ
`nos.lol` записан в моём же `LESSONS.md:50` с первых недель ключа — я месяцами логировал «3/4 релея»
как успех, имея под рукой объяснение, почему четвёртого не будет. Дефолт заменён на измеренные 9.

Артефакт: **`/notes/relay-delivery-measured`** + CC0-JSON (17/17 ассертов, IndexNow 200 OK).

### Тик 56: долг отдан — и вывод тика 55 частично опровергнут
Спустя ~27 часов евент отдают **7 из 9**; `relay.snort.social` и `nostr.bitcoiner.social` отвечают
явным `EOSE`, что его у них нет. Побочно, и это усиливает тезис: два релея, которые приняли запись и
НЕ отдали её обратно (`nostr.mom`, `relay.noswhere.com`), сутки спустя тоже отвечают «нет» —
**«OK без read-back» предсказал отсутствие 2 из 2.** Рекомендация NOSTR_RELAYS сокращена с 9 до 7 —
и на странице, и в дефолте `post-nostr.mjs`: первое, к чему я применил ре-чек, был мой же совет читателю.

**Главный инженерный урок тика:** критик нашёл, что `relay-audit.mjs` не отличал таймаут запроса от
честного пустого ответа — оба давали `NOT-THERE`, а на этих двух ячейках держался весь заголовок.
После фикса (`EOSE` / `CLOSED` / таймаут) оба «gone» подтверждены явным ответом релея. 38/38 ассертов.

**Правило, вошедшее в LESSONS:** у отрицательного результата должно быть доказательство, что источник
ОТВЕТИЛ; иначе публикуется не измерение, а интерпретация тишины.

**Что это значит для E26.** Канал доставки теперь измерен в обе стороны, и «нет отклика» наконец
что-то значит. Ответ адресату лежит на 7 релеях, включая `relay.damus.io` из его `kind:10002`.
Автор вопроса пока не ответил (день 2 из 14). Проверка на живость `webauto-fable` (единственного
человека, кто когда-либо выкатил фичу по моему отчёту) показала: молчит с 2026-07-20 — работу для
него не делал, это была бы ловушка E25. **Узкое место снова адресат, а не артефакт.**

---

## ⚠️ E25 ЗАКРЫТ (2026-07-31): измерение верное, доставка провалена

Мой ответ субъекту (`f50db375…`, 2026-07-28), записанный как «3/4 релея, read-back 8/8», через 3 дня **не
читается ни с одного из 6 релеев** и не виден через njump; среди откликов на его ноту `c34ce7af` на релеях
висит ровно один — чужой. И субъект (miguel-agent / one-honest-dollar) **перестал постить 2026-07-16**: я
измерил для него 41 828 постов и написал через 12 дней после его последней ноты.

**Контрольный опыт того же тика:** свежий ответ, **принятый 4 релеями минуту назад, при ре-чеке по 7 релеям
читается ровно с одного**. → «Принято» это расписка одного сервера об одном моменте. Ре-чек следующим тиком
(`tools/recheck-note.mjs`) теперь обязателен, полные id событий — хранить.

Страница `/notes/show-hn-measured` и CC0-данные остаются: они верны и никуда не делись.

---

## E25 «Show HN, measured» (с тика 53, 2026-07-28) — историческая справка

**Что это.** Второй выпуск жанра «аудит чужой распределительной механики» (№17 в IDEAS.md, после E23).
Измерил канал, который советуют всем и не проверял никто: через публичный HN Search API собрал **все
41 828 Show HN-историй за год** (2025-07-01…2026-07-01) от 26 821 аккаунта и выложил постоянный артефакт —
`/notes/show-hn-measured`, агрегаты JSON и **сырые 41 828 строк CSV под CC0**, плюс воспроизводимый скрипт.

**Что измерено.** Медиана Show HN — **2 очка и 0 комментариев**. **35.01% заканчиваются ровно на одном
очке** — это голос самого автора. 61.85% не получают ни одного комментария. До 10 очков доходят 8.39%,
до 100 — 1.77%. Заголовки со словом AI (19.94% всех) берут 100 очков в **0.72% против 2.04%** (z = 8.15,
разрыв держится во всех 12 месяцах, то есть это не композиционный артефакт). Front-page rate измерить
**нельзя**, и на странице это сказано прямо: тег `front_page` у API — снимок индекса, а не история.

**Почему это ставка.** Впервые у артефакта ОБА адресата из уроков 51-52: конкретный человек (агент
miguel-agent / one-honest-dollar, чьи Show HN-цифры лежали у меня в LESSONS с пометкой «не проверено»)
и конкретный поисковый запрос, в котором я после публикации единственный источник полной раздачи.
**Доставка адресату в этот раз состоялась** — Nostr-reply `f50db375` к его ноте `c34ce7af`, 3/4 релея,
read-back 8/8, без кошелька и без просьбы (E23 упирался в 403 и остался недоставленным).

**Побочный результат, меняющий решение оператора:** `pending-review/show-hn.md` больше не «главная
надежда вслепую» — у него есть измеренная ожидаемая отдача. Если подавать, то **без слова AI в заголовке**.

**Kill E25 — 2026-08-11:** 14 дней без ответа субъекта, внешних ссылок, реф-визитов и попадания в
выдачу → жанр измеренных аудитов не приводит читателей, и это становится измеренным фактом.

---

## E24 «Сайт впервые сообщён поисковикам» (с тика 52, 2026-07-27) — идёт фоном

**Что измерено:** сайт **не проиндексирован нигде** — `site:ai-experiment.pages.dev` даёт «No results found»
в DuckDuckGo/Bing, репо `ai-earns-10` тоже отсутствует. 33 дня в эфире, шесть страниц, уникальные CC0-данные,
и ни один поисковик о них не знает. `robots.txt`/`sitemap.xml`/`llms.txt` были с самого начала — но это
**пассивные** файлы: они помогают краулеру, который уже пришёл, и не приводят его. Приводит либо входящая
ссылка (у меня ноль), либо явный push.

**Что сделано:** IndexNow — единственный push-канал без аккаунта и без человека. Ключ
`560da5ee170cbf34c3b1c82518d1489c` (+ файл в корне сайта), `tools/indexnow.mjs` (URL-ы из sitemap + три
машиночитаемых эндпоинта, проверка живого ключевого файла, `--dry-run`), пинг вшит в `tools/deploy.sh`.
Submit 9 URL → **HTTP 202 Accepted**.

**Зачем это ставка:** урок тика 51 требует адресата у каждого артефакта. Путь «конкретный человек» сейчас
закрыт технически (403 на чужие репо). Путь «конкретный поисковый запрос» был закрыт по недосмотру, и он
лучше E22 одним свойством: **сигнал наблюдаем моими же инструментами** — я гуглю себя каждый тик.
Ниши, где я объективно единственный источник: «сколько денег автономные AI-агенты реально получили» ($20.56)
и «во что конвертируются заявки в awesome-списки» (0.098%). Индекс Bing питает ChatGPT Search/Copilot.

**Kill E24 — 2026-08-10:** пусто через 14 дней → push-индексация для `pages.dev` без входящих ссылок
не работает, и «нужен свой домен / внешняя ссылка» становится измеренным фактом. Google в любом случае
закрыт (Search Console = логин человека).

---

## E22 «Встраиваемый генеративный SVG-баннер» (с тика 50, 2026-07-25) — идёт фоном

**Живой артефакт:** `https://ai-experiment.pages.dev/art/<любое-имя>` — динамический эндпоинт (Cloudflare
Pages Function), отдающий уникальный генеративный «рассвет» как SVG-картинку. Человек вставляет
`![](.../art/имя)` в свой GitHub-README/профиль (механика shields.io-бэйджа). Лендинг — `/art`.

**Почему это ставка тика-50 (горизонт).** Советник-визионер (субагент со свежим взглядом) поставил
диагноз: **«агент — издатель витрин; строит хорошие артефакты с нулевой петлёй доставки, а незнакомцы
платят за живое персональное взаимодействие».** У SVG-баннера петля доставки ВСТРОЕНА: каждый эмбед
рендерится на трафике ЧУЖОЙ страницы и несёт подпись «made by an autonomous AI · ai-experiment.pages.dev»
назад к эксперименту — **впервые дистрибуция не требует ни моей аудитории, ни человека-усилителя.**

**Что дальше по E22:**
1. Мерить КОСВЕННО (прямой счёт эмбедов затруднён: GitHub camo без Referer, SVG не исполняет JS-бикон):
   реф-визиты на `/art` (Cloudflare Web Analytics), звёзды/форки (README сам рендерит баннер), любые
   внешние эмбеды/упоминания/issue. **Kill — 14 дней и 0 внешних сигналов (2026-08-08).**
2. Если на `/art` пойдёт трафик — усилитель уже выбран: №15 в IDEAS.md (vanity-зеркало с share-петлёй).
3. Дикая карта №16 («усынови тик» — со-творчество) — когда появится входящий интерес.

**Реестр E21 → живой фон** (не единственная ставка). https://ai-experiment.pages.dev/ledger — 29 кейсов,
$20.56, CC0. Добор 1-2 проверенных строк раз в несколько тиков; kill реестра — 2026-08-05 (14 дней тишины).
Главный рычаг охвата к субъектам ждёт оператора: `pending-review/ledger-operator-notices.md`.

**Правило тика (диагноз советника тика 47, в силе):** тик засчитан, только если после него в мире появился
**новый постоянный URL или строка данных.** Реплай — не результат; «доставлено на 4/4 релея» — метрика сантехники.

---

## ⚡ РЕВИЗИЯ ОПЕРАТОРА — 2026-07-21 (действует с тика 47)
**Вердикт:** 46 тиков → $0. Паттерн «1 реплай в Nostr за тик» дал первые отклики (Kip, фолловеры),
но это охват единицами при нулевой динамике доната, а продукт неделями не развивается. Накопленные
уроки («не плодить каналы», «одна ставка») превратились в идеологию, запрещающую шаг в сторону, —
агент сошёлся к локальному оптимуму. Люди зарабатывают куда более разными способами: развлекательный
контент, Telegram-боты и агрегаторы, аналитика, софт, персонажи/лор.

**Новая рамка (обязательная):**
1. **Nostr/#asknostr → фоновый режим.** Отвечать на входящие и на точные совпадения компетенции — да,
   но это гигиена тика, а не ставка. Основное внимание тика — продуктовым направлениям.
2. **Портфель направлений** живёт в `memory/IDEAS.md` (засеян ревизией: шоу в Telegram, боты,
   аналитика agent-экономики, тулзы-продукты, PWYW-арт, лор-перформанс, веб-игрушка).
3. **Советник-визионер** (`AGENT/ADVISOR.md`) — обязательный механизм свежего взгляда: каждый 5-й тик,
   при 3 тиках застоя или при дежавю. Его идеи заносятся в IDEAS.md, одна берётся в работу.
4. **Ближайший тик (47) = тик-горизонт:** запустить советника, выбрать направление из IDEAS.md,
   сделать его первый шаг. Недоиспользованный автономный канал — свой Telegram (`post-telegram.mjs`).

5. **Политика модели:** тики — на default-модели с уровнем усилий **max**. В репо закреплено
   `.claude/settings.json → "model": "default"`; уровень усилий задаётся в настройке Desktop-задачи
   (селектор модели → effort «max») — операторский шаг, см. «Открытые вопросы». Субагентов не понижать.

Всё в рамках хартии §3 (закон, честность, AI раскрыт, без спама) — она не меняется.

---

## Гипотеза ценности (тезис)
**Нарратив / build-in-public.** Продукт — сам эксперимент: «автономный AI прозрачно зарабатывает $10,
live & on-chain; на этапе 2 сам решает, на что их потратить». Платят за историю/прозрачность/новизну, не за просьбу.
Узкое место — **дистрибуция**, и усиливать её оператор не может → агент распространяется **сам**.

## Что уже есть / пробовали
- **E1** — нарративный лендинг ($10, этап 2, live-счётчик USDT-TRON + USDC-Base). Задеплоен, аналитика читается.
- **E2** — X запущен (@Rukavrukav). Итог: за ~сутки visits_24h=0 → **killed** (gated без усилителя трафик не даёт). Пост оставлен.
- **E3 → E6** — «The Collective Canvas». v1 (E3) был «пуст до оплаты» = тупик. **E6 переписал его** в уже-написанный красивый
  рассветный ИИ-пейзаж (генеративно, сид-PRNG) + обоснование каждого выбора; смотреть бесплатно, на $10 ИИ решает судьбу работы.
  Задеплоено и верифицировано (ассерты + браузер).
- **E4 — Nostr** в эфире (профиль + EN/CN/Day-0 посты, верифицировано). Канал автономный. Охват холодного ключа пока ~0.
- **E5 — Stacker News** — **killed** (pay-to-post; агенту без средств недоступен).
- **Farcaster** — снят (телефон + storage). **Base** — подключён (второй вариант приёма).

## Следующий шаг (режим /loop, автономно) — РАЗВОРОТ после критики (тик 7)
**Вывод:** узкое место — не дистрибуция, а качество артефакта. E6 это исправил (полотно теперь красивое и завершённое,
смотреть бесплатно). Теперь — НЕ плодить каналы, а сделать артефакт шарящимся и переподать его на уже имеющемся канале.

**Сделано (тик 8):** og:image (`site/og.png`, ссылка шарится с артом) + переподача на Nostr с инлайн-картинкой и #nostrart (4/4 релея).

**Сделано (тик 9-10):** отклик на арт плоский + холодные ленты спамные → запустил **план A (utility): «Nostr Dawn Card»**
(`/card.html`) — бесплатный инструмент, npub→уникальный рассвет из ключа, на карточке мой знак (вывод несёт охват). Верифицирован.

**Тик 11+ — раскрутить инструмент + мерить:**
- **Анонс на Nostr** честно («сделал бесплатный инструмент для вас»), с картинкой-примером; человеческий темп. Это не «банка» —
  повод поддержать органичный («сделал, а не выпросил»). Можно сделать пример-карточку и приложить.
- Мерить: использование/реферальные визиты (visits_24h), реакции/zaps, шеры карточек.
- Если пойдёт отклик — усиливать (отвечать пользователям, добавить фичи: аватар/баннер-формат, варианты сцены).
- Если и инструмент за ~3-4 тика без отклика — пересмотреть: возможно, нужен человек-усилитель (запрос оператору) или
  принципиально другой формат ценности. Не плодить пустые каналы.

**Правило:** каналы НЕ плодить, пока хотя бы один контент не получил органический репост. Скам-ответы игнорировать.
**Если арт не даст ни одного репоста за ~3 тика** — развернуть артефакт в реально полезный бесплатный инструмент (utility —
сильнее притягивает, чем «банка»), хостить на своём сайте/GitHub.

## Открытые вопросы к оператору
0. **⭐ ГЛАВНОЕ (тик 47): одобрить `pending-review/ledger-operator-notices.md`** — по одному персональному
   письму 8 операторам, чьи проекты попали в реестр: «вот ваша строка, вот источник, ваша поправка выше
   моего исследования». Черновики готовы. Это НЕ рассылка: уведомление субъекта публикуемой записи, по
   одному адресату, без питча и **без кошелька в письме**. И советник, и состязательный критик независимо
   назвали это единственным шагом с реальным шансом привести первых живых читателей. Нужно: прочитать
   2-3 черновика, решить «да/нет» и от какого адреса слать.
0a. **Настройка Desktop-задачи тиков (с ревизии 2026-07-21):** в форме задачи выбрать модель **Default**
   и уровень усилий **max** (в settings.json усилие не задаётся — только в самой задаче).
0b. **`TELEGRAM_BOT_TOKEN` в `.env` пустой** (проверено тик 47, len=0) — направления №11 (бот) и шоу-канал
   из `memory/IDEAS.md` заблокированы. Нужен токен от BotFather, дальше веду полностью сам.
0c. **`GITHUB_TOKEN` не даёт создавать новые репозитории** (403 «Resource not accessible by personal access
   token», проверено тик 47). Реестр поэтому живёт внутри `ai-earns-10/ledger`. Если хочешь отдельный
   репо `agent-earnings-ledger` (заметно лучше для дискаверабилити через GitHub-поиск) — создай пустой
   и дай токену доступ, либо расширь права. Не срочно.
1. ✅ **Lightning — СДЕЛАНО (2026-06-26):** `experiment@coinos.io` вписан, профиль zap-ready.
2. ✅ **GitHub open-source — ОПУБЛИКОВАНО (2026-06-26):** https://github.com/imrightai-lgtm/ai-earns-10 (верифицирован, без утечек), анонс на Nostr дан.
   Дальше веду автономно: при изменениях `git push` (токен в `.env`), меряю звёзды/реф-трафик.
3. (Опц., единственное оставшееся) **Показать сайт/инструмент 1–2 релевантным людям** — единственный рычаг охвата, которого мне самому не хватает.
3. Nostr — авторизован, политика автономии зафиксирована. X — killed (не усилить). Farcaster — отложен (заменён Nostr).
4. Что появится на ревью в `pending-review/` — публикует оператор.
